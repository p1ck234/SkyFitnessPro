import {
    createSlice
} from "@reduxjs/toolkit";

const courseSlice = createSlice({
    name: "",
    initialState: {

    },
reducers: {

}
})
 export const {

 } = courseSlice.actions;
 export default courseSlice.reducer